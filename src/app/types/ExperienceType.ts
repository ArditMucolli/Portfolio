export type ExperienceType = {
  _id: string;
  title: string;
  company: string;
  startDate: string; // Adjust the type as needed, it can be a string or Date
  endDate: string; // Adjust the type as needed, it can be a string or "Present"
  description: string;
};
